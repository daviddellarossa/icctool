Edit the run.bat file

Change the first path to point to the folder containing the images.
WARNING: it will process all TIFF image files in the folder. Running multiple times it will overwrite the previously generated _icc.tif files.

Change the second parameter with the full path to the .icc file to apply.

Execute the run.bat file.

The output files will be generated into the same source folder.